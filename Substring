#6 Program to check if a substring is present in a given string.
str1=input("Enter original string:")
str2=input("Enter substring to serach:")
index=str1.find(str2)
if index==-1:
 print("Substring not found!")
else:
 print("Substring fount at index:{}".format(index))
